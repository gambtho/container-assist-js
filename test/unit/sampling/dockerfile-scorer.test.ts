import { describe, it, expect } from '@jest/globals';

describe('Dockerfile Scorer (Legacy)', () => {
  // These tests are disabled as they test the old class-based system
  // The scoring functionality is now embedded in the functional sampling system

  it('should skip legacy scorer tests', () => {
    expect(true).toBe(true);
  });
});