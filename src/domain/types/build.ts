/**
 * Build process types
 *
 * The codebase currently uses DockerBuildResult and DockerBuildOptions from docker.ts.
 * Additional build tool types can be added here when implementing buildah/kaniko/podman support.
 */
